dubbo:
  application:
    name: ${spring.application.name}
  # Dubbo Registry
  registry:
    address: zookeeper://127.0.0.1:2181
  # Dubbo Protocol
  protocol:
    id: dubbo
    name: dubbo
    port: -1
  protocols:
    rest:
      name: rest
      port: -1
  provider:
    # Provider default Protocol
    protocol: dubbo
